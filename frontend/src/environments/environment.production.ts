export const environment = {
  production: true,
  supabaseUrl: '', // Set via build environment variable
  supabaseAnonKey: '', // Set via build environment variable
  apiBaseUrl: '/api/v1',
  wsBaseUrl: '/api/v1', // upgraded to wss:// by the browser when on https
};
